namespace HiddenTrail.Utilities
{
    public static class Constants
    {
        // --- Input ---
        public const float SwipeThreshold = 50f; // px
        public const float SwipeTimeLimit = 0.5f; // sec

        // --- Animation ---
        public const float TileRevealDuration = 0.25f;
        public const float PlayerMoveDuration = 0.12f;
        public const float PlayerMovePause = 0.05f;

        // --- Grid ---
        public const float TileSize = 1f;
        public const float TileSpacing = 0.05f;
    }
}